import { Suit } from '../types';

/**
 * Convert a suit into a string
 * @param suit The suit to be converted
 * @returns A string representing the given suit
 */
export function stringifySuit(suit: Suit): string {
	return suit;
}
